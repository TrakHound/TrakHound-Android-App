<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.trakhound.www.trakhound">

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <android:uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- <android:uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->
    <!-- <android:uses-permission android:name="android.permission.READ_PHONE_STATE" /> -->
    <!-- <android:uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> -->
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity -->
        <!-- android:name=".SigninActivity" -->
        <!-- android:label="@string/title_activity_signin" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->

        <activity
            android:name=".DeviceListActivity2"
            android:label="@string/title_activity_device_list"
            android:parentActivityName=".Home"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- <activity -->
        <!-- android:name=".Main2Activity" -->
        <!-- android:label="@string/title_activity_main2" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <!-- <activity android:name=".devices.GetDeviceActivity" /> -->

        <activity
            android:name=".DeviceListItemActivity"
            android:label="@string/title_activity_device_list_item"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- <activity -->
        <!-- android:name=".DeviceDetailsActivityListActivity" -->
        <!-- android:label="@string/title_devicedetailsactivity_list" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <!-- <activity -->
        <!-- android:name=".DeviceDetailsActivityDetailActivity" -->
        <!-- android:label="@string/title_devicedetailsactivity_detail" -->
        <!-- android:parentActivityName=".DeviceDetailsActivityListActivity" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value="org.trakhound.www.trakhound.DeviceDetailsActivityListActivity" /> -->
        <!-- </activity> -->

        <activity
            android:name=".DeviceDetailsActivity"
            android:label="@string/title_activity_device_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Home" />
        <activity
            android:name=".LocalLoginActivity"
            android:parentActivityName=".Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserLoginActivity"
            android:parentActivityName=".Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserHome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LocalHome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeDrawer"
            android:label="@string/title_activity_home_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DeviceList"
            android:label="@string/title_activity_device_list"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".About"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>